<!DOCTYPE html>
<html>
  <head>
    <% display_global_config = ui_global_config(@global_config) %>
    <title><%= display_global_config['INSTALLATION_NAME'] %></title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
    <script>
      window.globalConfig = <%= raw display_global_config.to_json %>
    </script>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'survey' %>
  </head>
  <body>
    <div id="app"></div>
    <%= yield %>
  </body>
</html>
